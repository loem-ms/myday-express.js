<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta http-equiv="content-type"
            content="text/html"; charset="UTF-8">
        <title><%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>

    <body>
        <head>
            <h1><%= title %></h1>
        </head>
        <div role="main">
            <p><%- content %></p>
            <form method="POST" action="/tasks/<%= form.id %>/edit">
                <input type="hidden" name="user_id" value="<%= form.user_id %>">
            <table>
                <tr>
                    <th>TITLE</th>
                    <td><input type="text" name="title" value="<%= form.title %>"></td>
                </tr>
                <tr>
                    <th>COURSE</th>
                    <td><input type="text" name="course" value="<%= form.course %>"></td>
                </tr>
                <tr>
                    <th>DEADLINE</th>
                    <td><input type="datetime-local" name="deadline" value="<%= form.deadline %>"></td>
                </tr>
                <tr>
                    <th>PRIORITY</th>
                    <td>
                        <select  name="priority">
                          <option value="Low">Low</option>
                          <option value="Normal">Normal</option>
                          <option value="High">High</option>
                        </select>
                    </td>
                </tr>
                <tr>
                    <th>COMMENT</th>
                    <td><textarea name="comment" ><%= form.comment %></textarea></td>
                </tr>
                <tr>
                    <th></th>
                    <td><input type="submit" value="update"></td>
                </tr>
            </table>
            </form>
        </div>
        <a href="/">Task List&gt;&gt;</a>
    </body>

</html>